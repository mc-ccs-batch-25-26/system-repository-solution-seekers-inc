# Leave VITE_API_URL unset for normal local development.
# Vite proxies /api requests to http://localhost:8000.
#
# Use this only when the frontend must call a different backend:
# VITE_API_URL=https://your-api.example.com/api
